using UnityEngine;

public class ModelNamesController : MonoBehaviour
{
    public string modelDialogue;      // for CharacterController conversation calls
    public string modelAddressing;    // for the addressing/routing call (cheap, fast model)
    public string modelGeneration;    // for CharacterGenerator name & description calls
}
